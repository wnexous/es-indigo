import PageParams from "@/interfaces/pageparams.interface"


export default function AdminPage({ params }: PageParams<{ role: string }>) {
    // const { role } = params
    // const { profile } = useProviders()
    // const userProfile = profile.getProfile()
    // const userRoles = userProfile?.roles
    // const havePermission = userRoles.includes(role)

    return <>Página sem conteúdo</>

}